export enum AccountColumns {
  ACCOUNT,
  DESCRIPTION,
  ICON,
  TYPE,
  ACTIONS,
  EDIT,
  DELETE,
}

export enum FormFieldType {
  INPUT = 'input',
  TEXTAREA = 'textarea',
  CHECKBOX = 'checkbox',
  DATE_PICKER = 'datePicker',
  SELECT = 'select',
  SKELETON = 'skeleton',
}
